# React Router / Vite SPA fallback
RewriteEngine On
RewriteBase /

# Não reescreve se o arquivo ou diretório existir
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redireciona tudo para index.html
RewriteRule . /index.html [L]
